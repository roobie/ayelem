
module.exports = {
  Range: function range (from, to, step) {
    return new Array(Math.ceil((to - from) / (step || 1)))
      .join(' ').split(' ')
      .map((_, i) => from + (i * (step || 1)))
  }
}
