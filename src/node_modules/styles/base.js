const csjs = require('csjs')

const _ = require('lodash')

module.exports = csjs`
* {
  box-sizing: border-box;
}

html, body,
button {
  font-family: monospace;
}

${_.range(1, 10).map(n => `h${n}`)} {
  margin: 0;
}
`
