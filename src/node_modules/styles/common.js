const csjs = require('csjs')

const vars = require('./vars')

module.exports = csjs`
.selected,
a.selected {
  font-weight: bold;
  color: seagreen;
}

.unselectable {
  user-select: none;
}

.clickable {
  cursor: pointer;
}

.disabled {
  cursor: not-allowed;
}
`
